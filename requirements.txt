# Root requirements — installs everything needed to run all services.
# For production, install only the service-specific requirements.txt instead.

# --- API (FastAPI backend) ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=8.3.0
redis>=5.0.4
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0
alembic>=1.13.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
resend>=0.7.0
stripe>=9.9.0

# --- Riot service ---
tenacity>=8.3.0

# --- LLM service ---
anthropic>=0.28.0

# --- CV service ---
ultralytics>=8.2.0
opencv-python>=4.9.0
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
matplotlib>=3.8.0
tqdm>=4.66.0
PyYAML>=6.0
